<header class="bg-white border-b">
    <div class="container flex items-center justify-between p-4">
        <a href="{{ route('index') }}" class="flex items-center gap-2 text-3xl font-bold">
            <x-icon name="circle-play" class="size-12" />
            {{ config('app.name') }}
        </a>
    </div>
</header>